# Command line argument for splitting instances of tornado that run on different ports

python index.py --port=8082

#split terminal
python index.py --port=8083

#split terminal
python index.py --port=8084
